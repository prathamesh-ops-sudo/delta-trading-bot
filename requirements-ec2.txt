# Lightweight Requirements for EC2 t2.micro (1GB RAM)
# Optimized for low-memory environments

# Data Processing
numpy==1.24.3
pandas==2.0.3
scipy==1.11.1

# Machine Learning (Lightweight versions)
scikit-learn==1.3.0

# TensorFlow Lite (Much smaller than full TensorFlow)
# We'll use tensorflow-cpu instead of full tensorflow (3x smaller)
tensorflow-cpu==2.13.0

# Alternative: Use these if tensorflow-cpu still fails
# keras==2.13.1

# Technical Analysis
TA-Lib==0.4.28

# API and Networking
requests==2.31.0
urllib3==2.0.4

# Scheduling
schedule==1.2.0

# Configuration
python-dotenv==1.0.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3
